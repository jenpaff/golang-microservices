pkgname  = "models"
output   = "persistence/models"
wipe     = true
no-tests = true

[psql]
  dbname = "postgres"
  host   = "localhost"
  port   = 5432
  user   = "my-user"
  pass   = "my-users-password"
  sslmode = "disable"
  blacklist = ["schema_migrations"]